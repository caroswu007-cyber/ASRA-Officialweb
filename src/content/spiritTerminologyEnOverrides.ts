/**
 * English spirit/Soul terminology overrides.
 *
 * All values have been flushed into docs/page-copy/*.xlsx (english_源文 column).
 * The xlsx workbooks (via npm run editor) are now the single source of truth
 * for all English page copy — including bold **markdown** formatting.
 *
 * This object is intentionally empty. Do not add entries here; edit the xlsx
 * workbooks directly or via `npm run editor`.
 */
export const EN_SPIRIT_TERMINOLOGY_OVERRIDES: Record<string, string> = {};
